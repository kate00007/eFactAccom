
(function () {
    'use strict';
    /*
    var app = angular.module('app');
        app.controller('MapController', ['$scope', '$http', '$routeParams', '$route', 'dataFactory', 'Utils', function ($scope, $http, $routeParams, $route, dataFactory, Utils) {

        $scope.init = function () {

            console.log("init MAP CONTROLLER");

        }

    } ]);

    */
} ());
